# M10.2 Validation Record

Date: 2026-08-13, Apple Silicon macOS 26.5.

## Deterministic baseline

- `pnpm check`: Core/Main/Preload/Renderer TypeScript passed; 113/113 tests passed;
- onboarding preference tests prove versioned defaults, private `0600` persistence and corrupt-state fail-safe;
- tutorial tests prove explicit unique creation, `0700` directory/`0600` files, safe reuse and no overwrite;
- an out-of-root remembered path is rejected and replaced with a new managed tutorial directory;
- workspace readiness detects Git/tutorial state without reading repository contents;
- Desktop channel alignment and security checks cover the new Main/Preload surface;
- template tests prove three bounded `balanced` drafts and explicit approval wording for Coding.

## Real Electron window

The development Electron window was inspected and operated through the macOS accessibility surface:

1. the first screen rendered a persistent “指引与示例” sidebar entry, readiness checks, three outcome cards, four runtime mechanisms and an offline Guide rail;
2. the UI explicitly stated that the Guide does not call a model, read files or start a task;
3. existing DeepSeek availability was shown only as a boolean ready state; no credential value was exposed;
4. clicking “创建教程工作区并预填” created a managed tutorial directory and changed the current workspace to it;
5. the Composer received the bounded tutorial goal while Run history stayed at zero and the “开始任务” button remained a separate user action;
6. the Coding template became disabled because the tutorial directory is not a Git repository;
7. returning to a real Git workspace enabled Coding readiness;
8. selecting an existing succeeded Run replaced the Guide with a state-derived hint to verify Artifact name, size and SHA-256.

No real Provider Run was started during this UI acceptance, so no model cost or data transmission was incurred. The earlier M10.1 real Research/Coding evidence remains the execution oracle; this validation is specific to first-run preparation and explanation.

## Package and installation

`pnpm make:mac` and `pnpm verify:mac-package` passed against the final `0.11.0` source:

| Artifact | Bytes | SHA-256 | Verification |
|---|---:|---|---|
| `LocalBuddy-0.11.0-arm64.dmg` | 224,031,108 | `379e8e49522c4f97cc22a436f0507675128ac395a4d994649c1b3d4924afb145` | DMG integrity; mounted app strict-deep ad-hoc signature; 14 relative symlinks; fuses; packaged browser; Renderer smoke |
| `LocalBuddy-darwin-arm64-0.11.0.zip` | 223,529,213 | `eff24f1153c3d51c20c79d6bbba38efc1e562cd04557039c24635ce0abd27d76` | Forge ZIP generation passed |

The matching app was installed at `/Applications/LocalBuddy.app`. Its `CFBundleShortVersionString` is `0.11.0`; installed `app.asar` SHA-256 is `9ec827afdb6e843bfabaa83abf95a66935b9e82bdd47f16f40dee15f0a06c8c5`, matching the verified Forge bundle. The installed app reopened the persistent Guide from the sidebar and rendered the same readiness/capability surface.

The previous `0.10.0` application was moved recoverably to the macOS Trash before installation. This remains an ad-hoc internal package, not a public signed or notarized distribution.

## Remaining gates

- validate the no-recent-workspace screen in a clean OS user profile in addition to deterministic coverage;
- run the tutorial’s first real Research task with a consenting dogfood user and record comprehension/friction, without treating one completion as the 7-14 day reliability gate;
- Windows and Linux graphical first-run surfaces remain unverified on real devices;
- M11 persistent multi-turn work remains unapproved and is not implied by the Guide conversation shape.
