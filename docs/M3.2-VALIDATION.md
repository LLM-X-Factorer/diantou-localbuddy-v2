# M3.2 Controlled Integration Validation Record

验收日期：2026-08-08<br>
Git：2.53.0<br>
Provider：DeepSeek API<br>
真实 Smoke Run：`m32-real-integration-smoke-v1`

## 自动验证

- `pnpm check`：TypeScript 与 35 项测试全部通过。
- `pnpm build`：Core、Electron Main/Preload 与 Renderer 生产构建通过。
- Renderer 产物：HTML 0.61 kB、CSS 12.58 kB、JS 203.67 kB（构建输出口径）。
- Integration Manager 覆盖组合成功、组合冲突、baseline 漂移、apply、新文件、commit、commit 失败回滚、未提交撤销和 Proposal 持久化。
- DesktopRunManager 覆盖从持久 Proposal 批准写回并撤销；Projection 覆盖重启后恢复 integration lifecycle；Preload channel 与 Main contract 保持一致。

## 真实 DeepSeek + 人工 CLI 批准

临时 Git fixture：`/tmp/localbuddy-m32-real-smoke.5yCsYU`。初始 HEAD 为 `43a9509ce17ee2d1fa8d6e893f47df026e47efaf`。

真实链路：

1. Orchestrator 生成 `implement-greet` 与 `update-docs` 两个独立 Code Worker；
2. 两个 Worker 分别修改 `src/greet.js` 与 `docs/usage.md`，各自通过 `git_diff_check`；
3. integration-preview 合并两个 patch，并通过 `git_diff_check + node_test`；
4. Proposal 进入 `awaiting_approval`；
5. CLI 显式提供 `--apply --commit-message`；
6. 控制器重新核对 clean HEAD 和组合 patch 后创建 commit `965f308417c3226f42d7494fab6e0a90c6d0c631`。

最终仓库：

- `git status --short --branch` 为 `## main`；
- commit 只包含 `docs/usage.md` 和 `src/greet.js`，共 5 行新增、1 行删除；
- `node --test`：1 项通过；
- 事件日志共 75 条，包含 preflight、awaiting approval、approved、applying 与 committed；
- `.localbuddy` 未检出 `sk-...` 形式凭证。

Artifact：

- `patches/implement-greet.patch`：SHA-256 `90979e69ae39bcb44dd823ff978afa1621ffac4d2a2274c16db08d987287f4ef`；
- `patches/update-docs.patch`：SHA-256 `36c20ae7efd9f8495aaad8217a8c9558ebfa2c08f0c95225b37ca3cf15415853`；
- `integration/combined.patch`：566 bytes，SHA-256 `00113c722a3cc9e20057dc704eecce7182cc6dd0b0709843e60f0e7e1e4d7e2d`；
- `coding-summary.md`：SHA-256 `36b532835e91cb6ebbc61049772e75cb9d46d141eb7edbbe9ab532c3ed8d1cb4`。

## Desktop 验收

Electron 从相同 JSONL 恢复出：

- 3 个成功 Task；
- 4 个登记 Artifact；
- `受控集成 · 已提交` 状态；
- `git_diff_check · node_test` 组合检查；
- 人工批准、写回和 commit 事件。

截图：`.localbuddy/ui/m32-desktop.png`（本地验收资产，不进入 Git）。

- 尺寸：2880 × 1718；
- SHA-256：`a6f3e942e9042a8dfa05273c05fb7945731e8bcd4463776db13f89e37992cf55`。

## 剩余风险

- Worktree 和固定命令 allowlist 不是 OS 沙箱；项目测试与 Git filter 仍可能执行仓库代码。
- `--no-verify` 意味着 LocalBuddy commit 不运行项目 hook；组合检查必须显式覆盖项目真正需要的验证。
- 已提交状态不提供自动 revert commit；需要使用正常 Git 审查和回滚流程。
- 本里程碑当时尚未实现自动 worktree 清理、对话 checkpoint 和不可信仓库隔离；其中显式 worktree 生命周期清理已在 M3.3 补齐，对话 checkpoint 与 OS 隔离仍未实现。
