# M3.4 Research Checkpoint Resume Validation Record

验收日期：2026-08-08<br>
Node：22+<br>
Provider：DeepSeek API

## 自动验证

- `pnpm check`：Core/Main/Preload/Renderer TypeScript 检查与 48 项测试全部通过。
- `pnpm build`：Core、Electron Main/Preload 与 Renderer 生产构建通过；Renderer 输出 HTML 0.61 kB、CSS 14.37 kB、JS 206.99 kB。
- checkpoint 契约覆盖 workspace/goal/plan 绑定、消息只追加、turn 单调与已完成 Task 不可重开。
- 工具回执覆盖完成结果复用、只读工具安全重试，以及含糊写入/执行工具阻断。
- Scheduler 覆盖已完成 Task output 恢复、未完成 Task 原 Agent 调度和依赖继续执行。
- 子进程故障注入在工具完成回执和 Task 消息游标落盘后执行 `process.exit(73)`；Desktop 对账后在同一 Run ID 恢复，原工具只出现一次 `tool.completed`，没有 `run.restarted`。
- Run Projection 覆盖 `interrupted -> run.resumed -> checkpoint.restored -> succeeded`。
- 通用 `sk-...` 凭证模式、占位符和尾随空格扫描无命中；DeepSeek Key 只从 macOS Keychain 读取。

## 真实 DeepSeek Checkpoint Resume

临时 workspace：`/var/folders/g3/_7z9kzg13_x37317scvsz_xr0000gn/T/localbuddy-m34-real-kTxbGI`。<br>
Run：`run-m34-real-5e64f333-90fa-4ef4-bcc9-826164db6c76`。

1. 真实 DeepSeek 规划一个读取 `evidence.md` 的 Worker 和串行 Integrator；
2. Worker 的 `read_file` 完成回执落盘并追加 `tool.completed` 后，故障注入立即让子进程以 73 退出，此时 Task 消息游标尚未推进；
3. 新 `DesktopRunManager` 追加 `run.interrupted`，投影 checkpoint 为 available，0 个 Task 完成、2 个 Task 待恢复；
4. 用户动作在原 Run ID 追加 `run.resumed`，恢复过程中 `read_file` 产生一次 `tool.reused`，没有第二次执行；
5. Run 以 `succeeded` 收口，共 35 个事件，其中 `run.resumed=1`、`run.restarted=0`、`tool.completed=2`、`tool.reused=1`；
6. 登记产物 `m34-real.md` 为 485 bytes，包含精确证据短语，SHA-256 `4991980c2fd405986015797fa4532526680cfa865720c0188e6a323e35b7e01b`。

## Desktop 验收

Electron 真实窗口展示同一个安全中断的 Research Run：

- Safe Recovery 卡显示 checkpoint 可用、0 个 Task 已完成、2 个 Task 待恢复；
- 同时提供“从 checkpoint 继续”和“从头重新运行”，没有把两种语义混在一起；
- 两个非终态 Task 均投影为“已中断”；
- event rail 保留模型、工具与第 14 个 `run.interrupted` 事件。

截图：`.localbuddy/ui/m34-desktop.png`（本地验收资产，不进入 Git）。

- 尺寸：2880 × 1718；
- SHA-256：`845737dc5111cc27249870a66e628f1409018d798dc6922eafa9b50510a7be5a`。

## 明确边界

- 当前仅 Research Run 可从 checkpoint 续跑；Coding Run 继续使用原请求重放与 worktree 生命周期管理。
- model-inflight 会从最后已落盘消息重新发起请求，可能产生重复模型费用。
- workspace 内容漂移或含糊的写入/执行回执会阻断续跑；界面保留从原请求重新运行。
- checkpoint 含 Prompt、模型消息和工具输出，必须继续留在被忽略的 `.localbuddy/` 私有目录。
